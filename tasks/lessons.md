# Lessons Learned

<!-- Format: [date] | what went wrong | rule to follow next time -->
<!-- Entries are added automatically after corrections. -->

[2026-03-23] | Tried Big Buck Bunny (1080p) first to verify 8x8 transform | Use small FATE conformance files first (fate-suite/h264-conformance/FRext/). BBB is for final confirmation only.
[2026-03-23] | Marked steps "completed" based on compilation + no regression, not pixel correctness | A new decode path must produce correct output on at least one targeted test file before being marked done.
[2026-03-23] | 8x8 intra pred generic loop formulas had boundary panics (t[i-1] when i=0, l[i+2] when i=6) | When using index arithmetic in loops, explicitly enumerate boundary values (i=0, i=max) and verify no OOB. Run adversarial review with concrete index values, not abstract reasoning.
[2026-03-23] | NNZ broadcast bug: wrote summed NNZ to all 4 sub-blocks of each 8x8 block, but FFmpeg only writes to the first (nnz[0] += nnz[1]+nnz[8]+nnz[9]) | When porting FFmpeg patterns, read the C code literally — `nnz[0] +=` means ONLY nnz[0] is modified, not a broadcast. The other 3 positions keep individual values.
[2026-03-23] | vert_right/hor_down prediction used wrong reference index formula (li=y-2x-1, should be y-2x-2) | Always validate 8x8 prediction formulas against FFmpeg's EXPLICIT hardcoded assignments (h264pred_template.c), not by extending the 4x4 generic formula. The 8x8 case needs the same indexing pattern but the offset differs.
[2026-03-24] | Dequant4Table::new had spurious transpose `raster_pos = (x>>2)|((x<<2)&0xF)` — FFmpeg stores at `x` directly | Flat scaling matrices (all-16) are symmetric under permutation, so transpose bugs are invisible until custom scaling matrices are used. Always compare FFmpeg C code line-by-line when porting index arithmetic, especially for table-building loops.
[2026-03-23] | CAVLC IDR slice returned InvalidData but the decoder silently continued with a corrupted frame | When slice decode fails, check decoder-level logs (RUST_LOG=wedeo_codec_h264::decoder=debug) FIRST to see if slices are even completing. This catches CAVLC desync immediately.
[2026-03-23] | Inter transform_size_8x8_flag was read unconditionally for all inter MBs with non-zero luma CBP, but FFmpeg gates it with dct8x8_allowed which restricts based on sub-partition types | FFmpeg's get_dct8x8_allowed() (h264_mvpred.h:157) must be matched: P_8x8 requires all sub_mb_type==0, B_8x8 restricts sub-8x8 and B_Direct without direct_8x8_inference_flag, B_Direct_16x16 requires direct_8x8_inference_flag.
[2026-03-23] | (Pattern, not correction) Plan-then-execute for known root causes completed dct8x8_allowed fix in ~15min with zero dead ends | When you can point to the exact FFmpeg function+line that differs, write a plan with exact line numbers, logic, and verification commands BEFORE coding. If you can't identify the exact FFmpeg divergence point, you're still in diagnosis mode — don't start coding.
[2026-03-23] | Tracing was gated behind compile-time features (#[cfg(feature = "tracing")], tracing-detail, optional tracing-subscriber). This made traces unavailable in release builds and required --features tracing everywhere. | NEVER use compile-time feature gates for tracing. Libraries depend on `tracing` unconditionally, binaries always init `tracing-subscriber`. Runtime control via RUST_LOG only. The tracing crate is near-zero-cost when no subscriber is active (single atomic load).
[2026-03-24] | Spent ~15 min algebraically proving dequant_4x4_flat vs dequant_4x4 equivalence when the answer was in one line of FFmpeg C code (STORE_BLOCK macro, h264_cavlc.c:564) | When analyzing FFmpeg normalization, read the ACTUAL C macro/function FIRST. Don't algebraically reason about the transform chain — FFmpeg's implementation may factor normalization differently from the spec. One `grep STORE_BLOCK` would have saved 15 minutes.
[2026-03-24] | `cargo check` updated timestamps but `cargo build` didn't recompile the binary, leaving a stale executable | After editing source files and running `cargo check`, always `touch` the edited file or run `cargo build` directly (not after `cargo check`) to ensure the binary is rebuilt. If output looks stale, `cargo clean -p <crate>` forces rebuild.
[2026-03-24] | For CAVLC 8x8 DCT MBs, deblocking used individual 4x4 sub-block NNZ values, but FFmpeg's `fill_filter_caches` (h264_slice.c:2396) overrides these with CBP-based values: if the 8x8 block was coded, ALL sub-blocks get non-zero NNZ for deblocking. Individual sub-block NNZ can be 0 even when the 8x8 block is coded. | When porting FFmpeg deblocking for 8x8 DCT, read `fill_filter_caches` not just `filter_mb_dir`. The NNZ cache is populated DIFFERENTLY for deblocking vs CAVLC NC context. The deblock needs "was the 8x8 block coded?" (CBP-based), not "how many coefficients in this 4x4 sub-block?" (decode-based).
[2026-03-24] | `mb_compare.py` defaults to `--no-deblock`, so running without `--deblock` flag showed all 300 frames matching when they don't | When using comparison scripts, always check which mode they default to. Use `--deblock` explicitly for deblock investigations.
[2026-03-24] | Compared wedeo deblock-on vs deblock-off to isolate filter changes, but the diff included MC cascade effects from deblocked references, not just the filter | To isolate the deblock filter itself, compare wedeo vs FFmpeg (both with deblock ON). Comparing same-decoder on/off includes MC reference differences.
[2026-03-24] | CABAC 8x8 decode read a coded_block_flag (CBF) that doesn't exist in the bitstream for cat=5, desyncing the CABAC engine for all subsequent symbols. All 20 FRext CABAC files showed 0/N frames matching. | For cat=5 (8x8 luma) in non-chroma-4:4:4, FFmpeg skips the CBF entirely (h264_cabac.c:1859: `cat != 5 || CHROMA444`). The CBP check is the only gate. Always read `decode_cabac_residual_nondc` in FFmpeg to check which cats have CBF and which don't.
[2026-03-24] | Chroma decode used `chroma_qp_index_offset[0]` for both Cb and Cr planes, ignoring `second_chroma_qp_index_offset` (PPS offset[1]). Files with different Cb/Cr QP offsets had V-plane-only diffs. | High profile PPS has `second_chroma_qp_index_offset` for Cr. Always compute per-plane chroma QP: `[CHROMA_QP_TABLE[...offset[0]...], CHROMA_QP_TABLE[...offset[1]...]]`. When diffs are isolated to one chroma plane, suspect QP offset handling.
[2026-03-24] | Spatial direct col_zero_flag optimization was applied unconditionally, but FFmpeg's h264_direct.c:374,405,443 gates it on `!sl->ref_list[1][0].parent->long_ref`. When L1[0] is a long-term reference, col_zero_flag must NOT suppress MVs to zero, because the colocated picture's ref_idx semantics differ for long-term frames. | When porting direct prediction checks, always read ALL conditions in FFmpeg's `if` statement, including `long_ref` checks. Single-frame B-frame diffs with large Y_max and correct DPB almost always point to direct prediction mode handling.
[2026-03-25] | PPS RBSP bit length used `data.len() * 8` which includes the RBSP stop bit padding. FFmpeg's `get_bit_length` (h2645_parse.c:348) strips trailing zero bytes then subtracts `ctz(last_byte) + 1`. This made `bits_left > 0` for Main profile PPS, reading garbage as high-profile extensions, silently failing PPS parse. | Always match FFmpeg's RBSP bit length computation. For any `bits_left` check, the bit length must exclude the stop bit and trailing alignment zeros. Use `rbsp_bit_length()` in pps.rs.
[2026-03-25] | Made ALL IDR frames barriers in delayed_pics, but this broke CVWP2/CVWP3 (single-IDR files) because the first frame's barrier changed output timing. | Only mid-stream IDRs (arriving when `delayed_pics` is non-empty) should be barriers. The first IDR has no prior frames to protect. Always test single-IDR files when changing barrier logic.
[2026-03-25] | Started session with dirty decoder.rs (100+ line reorder rewrite from previous session). Assumed my small warn! change was the only diff. Spent 15 min bisecting regressions caused by pre-existing changes. | At session start, always run `git diff --stat` on modified files. If there are pre-existing uncommitted changes, understand them BEFORE making new changes. Use `scripts/check_dirty_tree.sh`.
[2026-03-25] | For MBAFF, used a single save/restore for left-side NeighborContext, but the bottom MB of pair N overwrites the top MB's right edge before pair N+1's top MB reads it. Spent 3 hypotheses debugging CABAC desync that was actually caused by wrong left-neighbor modes for intra4x4 prediction. | MBAFF pair-interleaved decode requires SEPARATE left contexts for top and bottom rows. The top MB at (x+1,y) needs (x,y)'s right edge, and the bottom MB at (x+1,y+1) needs (x,y+1)'s right edge. A single save/restore misses this — need two separate saved states (top_left and bot_left).
[2026-03-25] | MBAFF CABAC desync: wedeo used frame-mode CABAC context offsets (sig_coeff=105+, last_coeff=166+) for ALL MBs, but field-coded MBs (mb_field_decoding_flag=1) need field-mode offsets (sig_coeff=277+, last_coeff=338+). Also hardcoded above_pair_is_field=false in field flag context, causing cascading wrong field flag values. Root cause was identified by extracting FFmpeg's per-block CABAC state via fprintf, finding `MB_FIELD=1` while wedeo had `mb_field=false`. | FFmpeg's `decode_cabac_residual_internal` uses `significant_coeff_flag_offset[MB_FIELD(sl)][cat]` — a 2D table indexed by field/frame mode. When porting CABAC residual, ALWAYS check whether context offsets vary by MB_FIELD. Also, `decode_cabac_field_decoding_flag` must look up the ACTUAL above pair's field flag from stored MB data, not hardcode false.
[2026-03-25] | Assumed CAMA1_Sony_C (MBAFF) was all-frame-mode and tested field-mode deblocking code against it expecting improvement. Wasted time when max pixel diff stayed at 255. Running mbaff_field_map.py showed 34% field-mode MBs, but the first differing MB (0,1) is frame-mode. | Before testing MBAFF changes against a specific file, run `python3 scripts/mbaff_field_map.py <file>` to check which MBs are field-mode. Don't assume field/frame mode from the file name.
[2026-03-25] | In MBAFF deblocking, initially applied bS=3 for interlaced intra edges in ALL directions (vertical + horizontal). FFmpeg only applies bS=3 for horizontal (dir=1); vertical (dir=0) always gets bS=4 due to the `FRAME_MBAFF && dir==0` upgrade path. | Read FFmpeg conditional logic fully — `!A || (B && C)` has two upgrade paths. The `FRAME_MBAFF && dir==0` clause means vertical always upgrades to 4, even with interlaced MBs.
[2026-03-25] | framecrc_compare.py reported "BITEXACT (0 frames)" for CVMA1/CVMAQP2/CVMAQP3 — files where wedeo produces 0 frames but FFmpeg produces 5-17 frames. `compare_one` uses `total = min(len_wedeo, len_ffmpeg)`, so 0 wedeo frames makes total=0, diffs=[], which looks like BITEXACT. | A 0/0 "BITEXACT" is meaningless — always compare against FFmpeg's actual frame count. `compare_one` must report frame count mismatch when counts differ, and precommit/conformance must treat count mismatches as failures, not passes.
[2026-03-25] | Intra4x4/8x8 top-right availability for the rightmost block column only checked `mb_x + 1 < mb_width` (bounds). In MBAFF, MB(x+1, y-1) may not be decoded yet when decoding the bottom MB of pair (x, y). This caused reads from uninitialized pixels, with characteristic right-edge-only diffs (cols 13-15). | Always use `slice_table[tr_idx] == current_slice` to check neighbor MB availability, not just bounds checking. This applies to both 4x4 and 8x8 intra prediction top-right availability.
[2026-03-25] | All 5 WP-1 "progressive FRext" files (FREXT01, FREXT02, FRExt2, FRExt4, Freh7) turned out to have frame_mbs_only=false. The plan incorrectly classified them as progressive. | Before investigating conformance files, always check SPS flags (frame_mbs_only_flag, mb_aff) with `RUST_LOG=wedeo_codec_h264::decoder=debug`. Don't trust file naming or ffprobe field_order alone.
[2026-03-25] | Simulated CABAC field flag decode with CABAC_BITS=17, got wrong result (MPS=0). Wedeo uses CABAC_BITS=16 (matching FFmpeg). With CABAC_BITS=16 the LPS path IS correctly taken, giving bit=1 (field mode). Wasted 20+ min debugging a "divergence" that was actually correct behavior. | When simulating CABAC manually, check `const CABAC_BITS` in cabac.rs first. It's 16, not 17. The shift in range comparison is `<< (CABAC_BITS + 1)` = `<< 17`, but CABAC_BITS itself is 16.
[2026-03-25] | When comparing FFmpeg vs wedeo CABAC state via lldb, breakpoint at ff_h264_decode_mb_cabac entry (line 1922) shows range offset because mb_field_decoding_flag hasn't been decoded yet. Used line 1966 (after field flag) and values matched perfectly. Wasted 10 min on false "CABAC init is wrong" hypothesis. | For MBAFF CABAC state comparison, always set FFmpeg breakpoint at h264_cabac.c:1966 (fill_decode_neighbors call), not at the function entry. This is after the field flag decode, matching wedeo's MBAFF_MB_START trace point.
[2026-03-25] | Verification script for left_block_options tables matched comment text instead of code values because regex `\[(\d[\d\s,]*)\]` matched numbers inside `// option 0: [3, 7, 11, 15]` comments. | When parsing Rust const arrays with regex, always strip `//` comments before extracting values: `re.sub(r"//[^\n]*", "", body)`.
[2026-03-26] | MBAFF CABAC intra4x4 mode prediction used a single-row `top_intra4x4_mode` buffer from NeighborContext, which the top MB of a pair overwrites before the bottom MB reads the previous pair's data. Both top and bottom field MBs got wrong top-neighbor modes. | For MBAFF, any per-row context buffer (modes, NNZ) is wrong for the second MB of a pair. Use per-MB storage indexed by the MBAFF-adjusted neighbor index (`nb.top_idx` from `compute_mbaff_neighbors`), not single-row buffers. If a single-row buffer must be used, save before the top MB and restore for the bottom MB.
[2026-03-26] | MBAFF field-mode top-right availability check for block (3,0) used `(mb_y-1)*mb_width+mb_x+1` to check if the above-right MB was decoded. But for field-mode MBs, the "above" row is from the previous pair row (mb_y-2), which is always fully decoded by the time the current pair row is processed. This caused top-right to be incorrectly unavailable, forcing diagonal prediction modes to replicate the last top pixel instead of using the actual above-right pixels. | For MBAFF field mode, any "above" row reference uses field stride (2×picture stride), landing in the previous pair row. Use `mb_y-2` instead of `mb_y-1` for availability checks that reference the above row.
[2026-03-26] | Spent ~60% of session inspecting deblock filter code (formulas, tables, pixel addressing) looking for the MBAFF deblock diff root cause. Every path looked identical to FFmpeg. Should have extracted ground truth values via lldb after 15 minutes. | After 15 minutes of code inspection showing no divergence across 3+ independent code paths, STOP reading code and extract ground truth values (lldb breakpoints, trace logging). One pixel extraction > one hour of algebraic analysis.
[2026-03-26] | Plan stated "luma max_diff=1" but framecrc_compare showed Y_max=5 after the fix. Worried the fix made things worse. Comparing both orders with the SAME tool (framecrc_compare --pixel-detail) showed column-first was strictly better (Y≤8 vs Y≤10). The plan's measurement used a different tool/metric. | Always compare before/after with the exact same tool and flags. Different scripts may report different metrics (per-MB max vs per-frame max).
[2026-03-26] | lldb Python script used `frame.FindVariable("linesize")` on an `av_always_inline` function, returning garbage (139 instead of 1440). Spent 40+ min analyzing "FFmpeg doesn't filter edge 1" based on wrong pixel values. | For inlined functions in lldb, NEVER trust `FindVariable` — use `expression` commands directly (e.g., `expression -f d -- (int)linesize`). Cross-validate any lldb extraction with at least one known-good value before building analysis on it.
[2026-03-26] | Traced MB(17,4)/(17,5) DEBLOCK_FINAL → zero diffs. But raw YUV had diffs at picture (276,92). The pixel was overwritten by MB(17,6)'s MBAFF horizontal edge 0 handler (a later pair modifying 3 rows into the above pair). | When a pixel matches in the MB's own DEBLOCK_FINAL but differs in raw output, check whether a LATER MB's edge 0 overwrites it. Use the pixel watchpoint pattern (before/after around deblock_mb) to find the modifying MB instantly.
[2026-03-26] | mb_compare.py reported "max_diff=1, mean_diff=1.0" for frame 1, which I assumed applied to ALL 89 differing MBs. It only reports the FIRST differing MB's stats. The actual frame had diffs up to 159 across many MBs. | Always check the TOTAL frame diff (framecrc_compare --pixel-detail or numpy on raw YUV). mb_compare's first-MB stats are NOT representative of the whole frame.
[2026-03-26] | Assumed "frame 1" in raw YUV was the P-frame. It was actually a B-frame (display order ≠ decode order). Wasted 20+ min computing MC for the wrong frame, getting nonsensical reference pixel values. | For streams with B-frames, ALWAYS run `python3 scripts/frame_type_map.py <file>` first to map display frames to decode order and slice types. Frame N in raw YUV is display frame N, not decode frame N.
[2026-03-26] | Tried swapping 8x8 IDCT pass order (row-first → col-first) to match what I thought FFmpeg does. Frame 0 immediately broke (Y_max=6). FFmpeg does col-first on TRANSPOSED data, which is effectively row-first on original data — same as wedeo. | Before changing IDCT pass order, ALWAYS verify by checking frame 0 (I-frame). If frame 0 breaks, the change is wrong. The transposition swaps the effective pass order: col-first on transposed = row-first on original.
[2026-03-26] | Intra 8x8 filtered top-right extension: previous fix changed ft[8..16] from raw top[7] to filtered ft[7]. But spec 8.3.2.2 says p[8..15]= p[7,-1] (unfiltered) BEFORE filtering, and avg3(v,v,v)=v, so filtering is identity. FFmpeg uses SRC(7,-1) (raw). The previous "fix" was wrong. | When porting PREDICT_8x8_LOAD_TOPRIGHT no-topright fallback: FFmpeg uses SRC(7,-1) (RAW pixel), NOT t7 (filtered). Read the actual C macro, not the variable name. t8..t15 = SRC(7,-1), not t7.
[2026-03-26] | CabacNeighborCtx stored -1 for inter/skip MB intra4x4_pred_mode, but NeighborContext (CAVLC path) stored 2 (DC). The CABAC intra mode prediction reads left from NeighborContext and top from CabacNeighborCtx — inconsistent storage caused wrong predicted mode when left had mode<2 and top was inter. | Two parallel neighbor systems (NeighborContext, CabacNeighborCtx) must store CONSISTENT values for intra4x4_pred_mode. FFmpeg's fill_decode_caches uses 2 (DC) for non-I4x4 neighbors without constrained_intra_pred. Check both systems when debugging CABAC neighbor issues.
[2026-03-26] | Spent 30+ min analyzing spec formulas and deblocking theory when the actual bug was in CABAC context neighbor storage — a data plumbing issue, not an algorithmic one. | When all formulas/algorithms look correct but output differs, check DATA PLUMBING: are the inputs to the correct algorithm coming from the right storage? Two systems storing the same concept differently is a classic source of bugs.
