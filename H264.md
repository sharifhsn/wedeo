# H.264 Baseline Decoder — Architecture & Status

## Overview

Native Rust H.264/AVC Baseline decoder (`codecs/wedeo-codec-h264/`) and Annex B
raw bitstream demuxer (`formats/wedeo-format-h264/`). ~15,100 lines of Rust,
309 unit tests, 0 clippy warnings. No unsafe code.

**Target**: Bit-exact decoded YUV420p output matching FFmpeg's FATE conformance
tests, verified via framecrc checksums.

**Current state**: End-to-end pipeline works — I-frames decode and produce
YUV420p output. Single-slice files (e.g., BA1_Sony_D.jsv) decode all 17 frames.
Multi-slice files have a CAVLC bitstream desync after the first slice.
Checksums don't yet match FFmpeg (pixel-level correctness still being debugged).

## Module Map

```
codecs/wedeo-codec-h264/src/
  lib.rs              18 lines   Module declarations
  nal.rs             587 lines   NAL unit parsing (Annex B + NALFF)
  sps.rs             998 lines   Sequence Parameter Set parsing
  pps.rs             628 lines   Picture Parameter Set parsing
  slice.rs           853 lines   Slice header parsing + MMCO/ref list mod
  cavlc_tables.rs    719 lines   VLC lookup tables (coeff_token, total_zeros, run_before)
  cavlc.rs         1,467 lines   CAVLC MB decode (residual, mb_type, prediction modes)
  intra_pred.rs    1,433 lines   17 intra prediction modes (4x4, 16x16, chroma)
  idct.rs            641 lines   4x4/8x8 IDCT, luma/chroma DC Hadamard transforms
  dequant.rs         422 lines   Pre-computed dequantization tables
  deblock.rs       1,797 lines   In-loop deblocking filter (bS, strong/normal)
  mc.rs            1,459 lines   Quarter-pel luma MC (6-tap FIR), eighth-pel chroma MC
  mvpred.rs          747 lines   Median MV prediction, P_SKIP, 16x8/8x16 rules
  refs.rs            592 lines   Reference list construction, MMCO, sliding window
  dpb.rs             508 lines   Decoded Picture Buffer (store/mark/output)
  mb.rs              632 lines   MB decode orchestrator (I_4x4, I_16x16, chroma)
  decoder.rs         759 lines   Decoder trait impl, multi-slice, avcC extradata
  tables.rs          285 lines   Zigzag scans, QP tables, CBP mapping, scaling matrices
  sei.rs              97 lines   SEI message parsing (recovery_point)

formats/wedeo-format-h264/src/
  lib.rs             460 lines   H.264 Annex B demuxer (probe, AU grouping)
```

## Decode Pipeline

```
.264 file → Demuxer → Packets (one per access unit)
                          ↓
              Decoder::send_packet()
                          ↓
              split_annex_b() → NAL units
                          ↓
              ┌─ SPS → sps_list[0..31]
              ├─ PPS → pps_list[0..255]
              ├─ SEI → ignored (parsed for recovery_point)
              ├─ AUD → ignored
              └─ Slice (IDR/non-IDR):
                    ↓
                  parse_slice_header() → SliceHeader
                    ↓
                  decode_slice_into(FrameDecodeContext):
                    ↓
                  for each macroblock:
                    ↓
                  decode_mb_cavlc()  → MacroblockCavlc
                    ↓                   (mb_type, pred modes, CBP, coefficients)
                  decode_macroblock():
                    ├─ I_4x4:
                    │    for each 4x4 block:
                    │      predict_4x4() → dequant_4x4() → idct4x4_add()
                    ├─ I_16x16:
                    │    predict_16x16()
                    │    luma_dc_dequant_idct() → per-block idct4x4_add()
                    └─ chroma:
                         predict_chroma_8x8()
                         chroma_dc_dequant_idct() → per-block idct4x4_add()
                    ↓
                  update neighbor context (nz counts, left/top)
                  store MbDeblockInfo
                    ↓
                  deblock_frame()  (after all MBs)
                    ↓
                  PictureBuffer → Frame (Y/U/V planes)
                    ↓
              Decoder::receive_frame()
```

## FFmpeg Reference File Mapping

| Wedeo module | FFmpeg source |
|-------------|---------------|
| nal.rs | libavcodec/h2645_parse.c |
| sps.rs | libavcodec/h264_ps.c (SPS part) |
| pps.rs | libavcodec/h264_ps.c (PPS part) |
| slice.rs | libavcodec/h264_slice.c (header parsing) |
| cavlc.rs | libavcodec/h264_cavlc.c |
| intra_pred.rs | libavcodec/h264pred_template.c |
| idct.rs | libavcodec/h264idct_template.c |
| deblock.rs | libavcodec/h264_loopfilter.c |
| mc.rs | libavcodec/h264qpel.c, h264chroma_template.c |
| mvpred.rs | libavcodec/h264_mvpred.h |
| refs.rs | libavcodec/h264_refs.c |
| decoder.rs | libavcodec/h264dec.c |
| format-h264 | libavformat/h264dec.c |

## Key Data Structures

### MacroblockCavlc (cavlc.rs)
Holds all syntax elements parsed from one macroblock:
- `mb_type`, `is_intra`, `is_intra4x4`, `is_intra16x16`, `is_pcm`
- `intra4x4_pred_mode[16]` — per-block prediction direction
- `cbp` — coded block pattern (luma 8x8 bits + chroma DC/AC bits)
- `luma_coeffs[16][16]` — dequantized 4x4 block coefficients
- `luma_dc[16]` — I_16x16 DC coefficients (pre-Hadamard)
- `chroma_dc[2][4]` — chroma DC per plane
- `chroma_ac[2][4][16]` — chroma AC per plane per block
- `non_zero_count[24]` — context for next MB's nC computation

### FrameDecodeContext (mb.rs)
Per-frame state during decode:
- `pic: PictureBuffer` — Y/U/V planes being filled
- `mb_info: Vec<MbDeblockInfo>` — deblocking metadata per MB
- `neighbor_ctx: NeighborContext` — CAVLC nC context (top/left nz counts)
- `qp: u8` — current QP (modified per-MB by qp_delta)
- `dequant4: Dequant4Table` — pre-computed dequant coefficients

### H264Decoder (decoder.rs)
Persistent decoder state:
- `sps_list[32]`, `pps_list[256]` — parameter sets
- `current_fdc` — in-progress FrameDecodeContext for multi-slice frames
- `output_queue` — decoded frames awaiting receive_frame()
- `nalff_length_size` — MP4/avcC NAL length size (0 = Annex B)

## CAVLC Decode Algorithm (cavlc.rs)

The core coefficient decoding follows H.264 spec section 9.2:

1. **coeff_token** — VLC from one of 4 tables (selected by nC context):
   - nC 0-1, 2-3, 4-7: variable-length codes
   - nC >= 8: fixed 6-bit code
   - nC = -1: chroma DC table
   - Returns (total_coeff, trailing_ones)

2. **Trailing ones** — 1 sign bit each (1=negative), most recent first

3. **Remaining levels** — prefix (leading zeros) + suffix:
   - `suffix_length` starts at 0 (or 1 if total_coeff > 10 && trailing_ones < 3)
   - First non-trailing level gets +2 offset when trailing_ones < 3
   - `suffix_length` increases when |level| exceeds threshold
   - Level sign: even level_code → positive, odd → negative

4. **total_zeros** — VLC (15 tables for 4x4, 3 for chroma DC)

5. **run_before** — VLC per coefficient (7 tables by zeros_left)

6. **Coefficient placement** — placed from highest scan position downward

## Known Issues

### CAVLC multi-slice bitstream desync
The decoder successfully decodes the first slice of each frame (up to 33 MBs),
but subsequent slices in the same frame fail with "Invalid data" errors.
Extensive investigation confirmed:
- All VLC tables match FFmpeg exactly
- Header bits tracking is correct
- nC context computation is correct
- The first 15 out of 16 blocks in the failing MB decode correctly
- A 1-2 bit desync accumulates across blocks, likely in the level prefix/suffix
  reading path for certain coefficient patterns

The desync does not affect single-slice files (all MBs in one NAL unit).

### Demuxer access unit grouping
The H.264 Annex B demuxer currently groups all NALs from the file into a single
packet instead of splitting per-frame. The decoder handles this correctly by
tracking frame boundaries via `first_mb_in_slice == 0`, but timestamps are
wrong (all frames get the same packet PTS). The decoder works around this by
using its own frame counter for PTS.

### P-frame inter prediction not wired
All building blocks exist (mc.rs, mvpred.rs, refs.rs, dpb.rs) but are not yet
connected to the MB decode path. P-frames currently fail because the decoder
only handles I_4x4 and I_16x16 macroblock types.

### Pixel-level bitexact parity
Even for successfully decoded I-frames, checksums don't match FFmpeg. Possible
sources of difference:
- IDCT rounding differences
- Dequantization scaling factor computation
- Intra prediction edge cases (neighbor unavailability handling)
- Deblocking filter threshold or pixel update differences

## FATE Conformance Test Files

Target: 17 Baseline profile tests from `fate-suite/h264-conformance/`:

| File | Description | Status |
|------|-------------|--------|
| AUD_MW_E.264 | Access Unit Delimiter | Multi-slice (blocked) |
| BA_MW_D.264 | Baseline Annex A | Multi-slice (blocked) |
| BA1_FT_C.264 | Baseline variant 1 | Multi-slice (blocked) |
| BA1_Sony_D.jsv | Baseline, 1 slice/frame | Decodes 17 frames |
| BA2_Sony_F.jsv | Baseline variant 2 | Needs testing |
| BA3_SVA_C.264 | Baseline variant 3 | Multi-slice (blocked) |
| BAMQ1_JVC_C.264 | Variable QP | Multi-slice (blocked) |
| BAMQ2_JVC_C.264 | Variable QP variant | Multi-slice (blocked) |
| BANM_MW_D.264 | Non-monotonic frame_num | Multi-slice (blocked) |
| BASQP1_Sony_C.jsv | Starting QP variation | Multi-slice (blocked) |
| SVA_BA1_B.264 | SVA Baseline variant | Needs testing |
| SVA_BA2_D.264 | SVA Baseline variant | Needs testing |
| SVA_Base_B.264 | SVA Baseline base | Multi-slice (blocked) |
| SVA_CL1_E.264 | SVA constraint level | Needs testing |
| SVA_FM1_E.264 | SVA frame mode | Needs testing |
| SVA_NL1_B.264 | SVA non-linear | Has P-frames (needs inter) |
| SVA_NL2_E.264 | SVA non-linear variant | Has P-frames (needs inter) |

## Next Steps (priority order)

1. **Fix CAVLC multi-slice desync** — build FFmpeg with CAVLC debug tracing,
   compare bit positions block-by-block to find the exact divergence point
2. **Wire P-frame inter prediction** — connect mc.rs + mvpred.rs + dpb.rs + refs.rs
   to the MB decode path in mb.rs
3. **Debug pixel-level differences** — compare per-MB pixel values against FFmpeg
   with a diagnostic tool
4. **Fix demuxer AU grouping** — split NALs into per-frame packets with proper
   timestamp assignment
5. **Write FATE integration tests** — automated comparison against FFmpeg reference
   output files
